# Artifact Virtual — Core Python Dependencies
# Install: pip install -r requirements.txt

# Encryption (Shield256 — AES-256-GCM)
cryptography>=42.0.0

# Password hashing (future auth module)
argon2-cffi>=23.1.0

# ─── Agent Runtime ────────────────────────────────────────
# Async HTTP client for LLM provider calls
httpx>=0.27.0

# Configuration & validation (v2)
pydantic>=2.0

# Agent YAML config loading
pyyaml>=6.0

# Cosine similarity for neural RAG embeddings
numpy>=1.26.0

# ─── OpenClaw Dependencies ────────────────────────────────
# OpenClaw AI gateway — Python skill runtime (local-places skill)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# OpenClaw docs tooling (optional)
mkdocs-material>=9.0.0
